# Public build-time switch. Set to live only after the database migrations
# and server-only Supabase variables are configured in the same Vercel project.
VITE_REQUEST_MODE=demo

# Server-only. Never prefix these with VITE_.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Legacy fallback only:
# SUPABASE_SERVICE_ROLE_KEY=
