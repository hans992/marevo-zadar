# Server-only preparation for the future live request intake.
# Do not configure or connect the public form until privacy, retention,
# rate-limiting and destination approval are complete.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Legacy fallback only:
# SUPABASE_SERVICE_ROLE_KEY=
