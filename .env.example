# Keep demo mode until a reviewed Supabase project and migration are deployed.
VITE_REQUEST_MODE=demo

# Server-only. Never prefix these with VITE_.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Legacy fallback only:
# SUPABASE_SERVICE_ROLE_KEY=
